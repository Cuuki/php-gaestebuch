{% extends "base.twig" %}

{% block head %}
    {% block title %}
        <title>Gästebuch</title>
    {% endblock %}
    {% block css %}
        <link rel="stylesheet" type="text/css" href="../css/bootstrap.min.css">
        <link href="../css/styles.css" rel="stylesheet">
    {% endblock %}
{% endblock %}
{% block header %}
{% endblock %}
{% block content %}
    <h1 class="heading">Willkommen in diesem Gästebuch</h1>
    <section id='posts'>
        <article>
            <h2 class='articleHeading'><a name='posts'>Letzte Beiträge:</a></h2>
        </article>
        {% if message is defined %}
            <p class="{{message_type}}">{{message}}</p>
            <br>
        {% endif %}
        {% if posts == false %}
            <p>Keine Beiträge vorhanden!</p>
        {% elseif errormessages is not defined or errormessages is null %}
            {% for post in posts %}
                <p>{{ post.firstname }}</p>
                <p>{{ post.lastname }}</p>
                <p>{{ post.email }}</p>
                <p>{{ post.content }}</p>
                <p>{{ post.created }}</p>
                <br>
            {% endfor %}
            {% include "pagination.twig" %}
        {% endif %}
    </section>
{% include "guestbookform.twig" %}
{% endblock %}